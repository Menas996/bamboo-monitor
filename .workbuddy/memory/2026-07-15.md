# 2026-07-15 工作日志

## 子项目详情页新增 Bamboo 部署历史模块

在 BuildDetail（子项目详情页）中新增 Deployments tab，完整对接 Bamboo 部署功能。

### 后端改动
- `electron/bamboo-client.ts`：新增 `stopBuild(buildResultKey)` 方法，同时尝试 `POST /build/admin/stopPlan.action`（停止运行中构建）和 `DELETE /rest/api/latest/queue/{buildResultKey}`（取消排队构建），与 deleteBuildResult 实现模式一致
- `electron/main.ts`：注册 `bamboo:stopBuild` IPC handler
- `electron/preload.ts`：在 `window.actions` 暴露 `stopBuild`

### 前端改动
- `src/App.tsx`：更新 window.actions 类型声明，新增 stopBuild，queueBuild 返回类型加 errorMessage
- `src/pages/BuildDetail.tsx`：新增 DeploymentsTab 组件（部署历史展示 + 中断/重试 + 5s 实时轮询 + 统计摘要 + 分页）
- `src/lib/i18n.tsx`：新增 14 个部署历史相关翻译键（中英文）

### 关键设计决策
- 项目服务器 Bamboo Deploy API 不可用（代码注释说明），部署概念基于 build results 映射（buildResultToDeploy），沿用此模式
- "中断"对接 Bamboo 原生 stopPlan.action + DELETE queue 两种途径，前端无需判断状态
- "重试"复用已有 queueBuild（重新触发构建）
- 运行中的部署 5s 轮询刷新状态，操作后立即刷新
- tsc + vite build 验证通过，零错误

## Bamboo 功能全量梳理

编写了 Bamboo 6.10.4 REST API 的完整功能盘点文档，输出到 `docs/bamboo-功能全量梳理.md`。
- 覆盖 13 个功能模块、200+ API 端点，每项标注增删改查操作、HTTP 方法、前置条件、约束限制
- 结合项目当前实现状态标注 ✅/❌/⚠️（已实现/未实现/服务器不可用）
- 包含实现优先级建议（P0-P3）、认证方式附录、Struts Action 调用模式、Bamboo Specs 说明、分页约定
- 关键发现：项目和计划的创建/删除不支持 REST API，需 Bamboo Specs；通知规则同理；部署 API 在当前服务器不可用
