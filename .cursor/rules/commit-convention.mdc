---
description: Git 提交信息规范 — [Tag]:中文说明
alwaysApply: true
---

# 提交规范

格式（单行主题，必填）：

```
[Tag]:中文说明
```

- `Tag`：固定英文标签（见下表），方括号包裹
- 冒号用半角 `:`
- 冒号后直接写中文说明，**不要**空格（`[修复]:中断状态误判`）
- 说明用中文，陈述改动目的或结果，禁止空洞词（如「更新代码」「改一下」）
- 主题尽量不超过 50 个汉字；需要补充时在空行后写正文

## 可用 Tag

| Tag | 用途 |
|-----|------|
| `功能` | 新能力、用户可见功能 |
| `修复` | Bug 修复、错误行为纠正 |
| `安全` | 鉴权、凭证、IPC、开放 URL 等安全相关 |
| `重构` | 无行为变化的结构整理 |
| `样式` | UI/视觉/文案展示（不含逻辑） |
| `性能` | 性能优化 |
| `构建` | 构建、打包、CI、脚本 |
| `配置` | 工程配置、依赖、规则 |
| `文档` | 文档、注释说明 |
| `测试` | 测试增删改 |
| `杂项` | 无法归入以上时的小改动 |

## 示例

```
[功能]:子项目详情增加部署历史状态条与中断操作
[修复]:点击历史记录不再自动跳回最新构建
[安全]:加固 openUrl 与配置 IPC，默认关闭 Git 自动部署
[重构]:统一构建状态分类逻辑
[样式]:StatusBadge 支持已中断状态展示
[构建]:补充 macOS DMG 打包脚本
[配置]:新增中文提交信息规范
```

## Agent 约定

- 用户要求创建 commit 时，**必须**使用本格式
- 一次提交只对应一个主 Tag；多类改动时选最主要的 Tag，或拆成多次提交
- 不要把无关文件（如本地 `.npmrc`）打进提交
